import React, { Component } from 'react';

export default class ColorBox extends Component {

  state = {
    todos: [
      
    ]
  }

  render() {
    return (
      const newValue = this.props.opacity
      return newValue < 0.2 ? null : (
      <div className="color-box" style={{opacity: 0.5 /*replace null with the value*/}}>
        {/* your conditional code here! */ this.props.opacity={0.2} ? this.props.opacity - 0.1 : null}
      </div>
      )
    )
  }

}

